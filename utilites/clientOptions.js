const { Collection } = require("discord.js")

const options = [
    { commands: new Collection() },
    { buttons: new Collection() },
    { selectmenues: new Collection() }
]

module.exports ={ options }